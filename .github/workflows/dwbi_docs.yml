name: Build DW&BI Docs

on:
  push:
    branches: [main]
    paths:
      - 'II/DWBI/docs/analysis/**'
      - '.github/workflows/dwbi_docs.yml'

defaults:
  run:
    working-directory: II/DWBI

jobs:
  build-docs:
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'push' && contains(github.event.head_commit.message, 'DWBI'))
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up TeX Live (XeLaTeX)
        uses: xu-cheng/latex-action@v4
        with:
          root_file: main.tex
          working_directory: II/DWBI/docs/analysis
          latexmk_use_lualatex: false
          latexmk_use_xelatex: true
          latexmk_shell_escape: false
          compiler: latexmk
          
      - name: Publish Analysis Report as release (package)
        working-directory: ${{ github.workspace }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cp II/DWBI/docs/analysis/main.pdf II/DWBI/docs/analysis/DWBI_Project.pdf
          gh release delete dwbi-docs-latest --yes 2>/dev/null || true
          gh release create dwbi-docs-latest \
            --title "DW&BI Project (latest)" \
            --notes "Built from ${{ github.ref_name }} at ${{ github.sha }}." \
            II/DWBI/docs/analysis/DWBI_Project.pdf

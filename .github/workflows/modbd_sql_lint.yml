name: Lint MODBD Oracle scripts

on:
  push:
    branches: [main]
    paths:
      - 'II/MODBD/sv*/scripts/**'
      - '.github/workflows/modbd_sql_lint.yml'
  pull_request:
    branches: [main]
    paths:
      - 'II/MODBD/sv*/scripts/**'
      - '.github/workflows/modbd_sql_lint.yml'

jobs:
  lint-sql:
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'push' && contains(github.event.head_commit.message, 'MODBD')) ||
      (github.event_name == 'pull_request' && contains(github.event.pull_request.title, 'MODBD'))
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install sqlfluff
        run: pip install sqlfluff

      - name: Lint SQL Scripts
        run: |
          sqlfluff lint II/MODBD/sv*/scripts \
            --dialect oracle \
            --exclude-rules RF05,LT12,CP02,LT05 \
            --templater raw

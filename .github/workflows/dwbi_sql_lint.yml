name: Lint DWBI Oracle scripts

on:
  push:
    branches: [main]
    paths:
      - 'II/DWBI/scripts/**'
      - '.github/workflows/dwbi_sql_lint.yml'
  pull_request:
    branches: [main]
    paths:
      - 'II/DWBI/scripts/**'
      - '.github/workflows/dwbi_sql_lint.yml'

jobs:
  lint-sql:
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'push' && contains(github.event.head_commit.message, 'DWBI')) ||
      (github.event_name == 'pull_request' && contains(github.event.pull_request.title, 'DWBI'))
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Install sqlfluff
        run: pip install sqlfluff

      - name: Lint SQL scripts
        run: sqlfluff lint --config II/DWBI/.sqlfluff II/DWBI/scripts
